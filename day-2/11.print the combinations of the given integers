print the combinations of  the given integers
program :

import itertools
numbers = [2, 4, 12]
for r in range(1, len(numbers) + 1):
    combinations = itertools.combinations(numbers, r)
    for combo in combinations:
        print(combo)

output:
(2,)
(4,)
(12,)
(2, 4)
(2, 12)
(4, 12)
(2, 4, 12)
